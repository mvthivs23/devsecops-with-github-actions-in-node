name: Analisis de Librerias

on:
  issues:
    types: [opened]

jobs:
  analyze_libraries:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Simular análisis de librerías
        id: check_library
        run: |
          # Simulando un análisis de librerías.
          RESULTADO_LIBRERIA="Libreria aceptada" # O "Libreria rechazada"
          echo "Resultado: $RESULTADO_LIBRERIA"

          if [[ "$RESULTADO_LIBRERIA" == "Libreria rechazada" ]]; then
            echo "Librería rechazada"
            exit 1  # Marca el trabajo como fallido
          else
            echo "Librería aceptada"
            exit 0  # Marca el trabajo como exitoso
          fi

      - name: Comentar en la issue si el análisis fue exitoso
        if: success()
        run: |
          curl -L \
          -X POST \
          -H "Accept: application/vnd.github+json" \
          -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
          -H "X-GitHub-Api-Version: 2022-11-28" \
          https://api.github.com/repos/${{ github.repository }}/issues/${{ github.event.issue.number }}/comments \
          -d '{"body":"🎉 El análisis de la librería ha sido aceptado."}' > /dev/null 2>&1

      - name: Comentar en la issue si el análisis falló
        if: failure()
        run: |
          curl -L \
          -X POST \
          -H "Accept: application/vnd.github+json" \
          -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
          -H "X-GitHub-Api-Version: 2022-11-28" \
          https://api.github.com/repos/${{ github.repository }}/issues/${{ github.event.issue.number }}/comments \
          -d '{"body":"⚠️ El análisis de la librería ha sido rechazado."}' > /dev/null 2>&1

      - name: Cerrar la issue como completada si fue exitoso
        if: success()
        run: |
          curl -L \
          -X PATCH \
          -H "Accept: application/vnd.github+json" \
          -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
          -H "X-GitHub-Api-Version: 2022-11-28" \
          https://api.github.com/repos/${{ github.repository }}/issues/${{ github.event.issue.number }} \
          -d '{"state":"closed", "state_reason": "completed"}' > /dev/null 2>&1

      - name: Cerrar la issue como no planeada si falló
        if: failure()
        run: |
          curl -L \
          -X PATCH \
          -H "Accept: application/vnd.github+json" \
          -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
          -H "X-GitHub-Api-Version: 2022-11-28" \
          https://api.github.com/repos/${{ github.repository }}/issues/${{ github.event.issue.number }} \
          -d '{"state":"closed", "state_reason": "not_planned"}' > /dev/null 2>&1
